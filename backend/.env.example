DATABASE_URL=postgresql://crm_user:crm_password@localhost:5432/crm_dev?schema=public
PORT=3000
NODE_ENV=development

# S2S JWT secret (must match frontend; generate with: openssl rand -base64 32)
S2S_JWT_SECRET=
